////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/////////////////////// คู่มือการใช้งาน ////////////////////////////////////////////////////////////////////////////////////////////

// swOK(); // wait for sw OK
// swSTART(); // wait for sw START

/////////////////////////////////////////////////////////////
/////////////////  Sensor (คำสั่งแสดงค่าเซนเซอร์) ///////////////
// ปรับ BuadRate -> 115200
// Serial_FrontSensor();            // Serial Monitor เซนเซอร์หน้า
// Serial_BackSensor();             // Serial Monitor เซนเซอร์หลัง
// Serial_CenterSensor();           // Serial Monitor เซนเซอร์กลาง
// -> Serial Monitor ค่าของเซนเซอร์ (ค่าดิบ)
// >>>> ค่าโดยประมาณ 200 - 900
// ==========================================================
// SerialCalibrate_FrontSensor();   // Serial Monitor เซนเซอร์หน้า คาริเบทแล้ว
// SerialCalibrate_BackSensor();    // Serial Monitor เซนเซอร์หลัง คาริเบทแล้ว
// SerialCalibrate_CenterSensor();  // Serial Monitor เซนเซอร์กลาง คาริเบทแล้ว
// -> Serial Monitor ค่าของเซนเซอร์ (ค่าที่ Calibrate แล้ว)
// >>>> ค่าโดยประมาณ 0 - 1000
// ==========================================================
// CalibrateSensor();
// -> Serial Monitor Calibrate ค่าแสงของหุ่นยนต์ (ทำในสนามครั้งเดียว)
// ตั้งค่วามเร็ว Serial Monitor >> 115200
// วิธีคาริเบทหุ่นยนต์
// 1) Upload คำสั่ง CaliberateRobotSensor();
// 2) เปิด Serial Monitor ให้แสดงคำว่า Ready
// 3) กดปุ่ม OK  ถูหุ่นยนต์บนสนามผ่านเส้น ขาว-ดำ อย่างช้าๆ *ห้ามยกลอย*
// 4) คัดลอกค่า SensorValue ไปใส่ในหน้าแรก เสร็จสิ้น

/////////////////////////////////////////////////////////////
/////////////////  Move (คำสั่งเคลื่อนที่) ////////////////////////
// คำสั่งเคลื่อนที่ Move(ความเร็วมอเตอร์ซ้าย,ความเร็วมอเตอร์ขวา,เวลาที่ใช้);
// Speed -> ความเร็ว 0-100
// Time -> เวลาที่ใช้งาน หน่วย Millisecond >> 1000 = 1 วินาทีโดยประมาณ
// ==========================================================
// คำสั่งทดสอบมอเตอร์
// -> ซ้ายเดิน ขวาหยุด -> ซ้ายหยุด ขวาเดิน -> ซ้าย ขวา หยุด
// Move(30 , 0 , 500);
// Move(0 , 30 , 500);
// Move(0 , 0 , 500);
// ==========================================================
// ToCenter();      //เคลื่อนที่ไปด้านหน้าจนกว่าเซนเซอร์กลาง ซ้ายหรือขวา จะเจอเส้น
// ToCenterL();     //เคลื่อนที่ไปด้านหน้าจนกว่าเซนเซอร์กลาง ซ้าย จะเจอเส้น
// ToCenterR();     //เคลื่อนที่ไปด้านหน้าจนกว่าเซนเซอร์กลาง ขวา จะเจอเส้น
// BackCenter();    //เคลื่อนที่ไปด้านหลังจนกว่าเซนเซอร์กลาง ซ้ายหรือขวา จะเจอเส้น
// BackCenterL();   //เคลื่อนที่ไปด้านหลังจนกว่าเซนเซอร์กลาง ซ้าย จะเจอเส้น
// BackCenterR();   //เคลื่อนที่ไปด้านหลังจนกว่าเซนเซอร์กลาง ขวา จะเจอเส้น

// ==========================================================

/////// คำสั่งออกตัว ///////////////////////////////////////////
/////////////////////////////////////////////////////////////
// คำสั่งเคลื่อนที่ไปจนกว่าจะพ้นจุด Start
// GoStart(ความเร็วมอเตอร์ซ้าย,ความเร็วมอเตอร์ขวา);
// GoStart(40,40);
// GoStart(40);
// ==========================================================
// คำสั่งเคลื่อนที่ไปจนกว่าจะถึงจุด Finish
// GoEnd(ความเร็วมอเตอร์ซ้าย,ความเร็วมอเตอร์ขวา);
// GoEnd(40,40);
// GoEnd(40);
// ==========================================================


//คำสั่งตั้งหุ่นยนต์ให้เกือบตรง
//BalanceF(จำนวนครั้งที่ต้องการจัดหุ่นยนต์ให้ตรง);
//BalanceF(1);
// ==========================================================
//BalanceB(จำนวนครั้งที่ต้องการจัดหุ่นยนต์ให้ตรง);
//BalanceB(1);
// ==========================================================

////////////////////////////////// คำสั่งเลี้ยว //////////////////////////////////////////////////////////////////
// >> SpinL();                 // หมุนหุ่นยนต์ด้านซ้าย
// >> SpinL2();                // หมุนหุ่นยนต์ด้านซ้าย 2 แยก
// >> SpinR();                 // หมุนหุ่นยนต์ด้านขวา
// >> SpinR2();                // หมุนหุ่นยนต์ด้านขวา 2 แยก

////////////////////////////////// คำสั่งวิ่งนับแยก //////////////////////////////////////////////////////////////////
// >> คำสั่งเจอแยก 'l'/'L' = กลางหุ่นเลี้ยวซ้าย , 'r'/'R' = กลางหุ่นเลี้ยวขวา  , 'p'/'P' = ตรง     , 's'/'S' = หยุด
// >> 'c'/'C' = กลางหุ่น , 'q' = หน้าเลี้ยวซ้าย , 'e' = หน้าเลี้ยวขวา ,'x' = ไม่ต้องทำอะไร

// >> FFC(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่กลาง
// >> FFL(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่ซ้าย
// >> FFL0(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์ริมซ้ายสุดตัวที่ 0
// >> FFR(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่ขวา
// >> FFR15(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์ริมขวาสุดตัวที่ 15
// >> FFNUM(ความเร็ว, 'คำสั่งเจอแยก', 0->7);  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >> ตำแหน่งเซนเซอร์ที่ใช้จับแยก

// >> BBC(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหลังเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่กลาง
// >> BBL(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหลังเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่ซ้าย
// >> BBL0(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหลังเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์ริมซ้ายสุดตัวที่ 0
// >> BBR(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหลังเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่ขวา
// >> BBR15(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหลังเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์ริมขวาสุดตัวที่ 15
// >> BBNUM(ความเร็ว, 'คำสั่งเจอแยก', 0->7);  // วิ่งตามเส้น PID ไปด้านหลังเจอแยกให้ทำตามคำสั่ง >> ตำแหน่งเซนเซอร์ที่ใช้จับแยก

// >> FFtimer(ความเร็ว,เวลา);  // วิ่งตามเส้นด้วย PID Timer ไปข้างหน้า
// >> BBtimer(ความเร็ว,เวลา);  // วิ่งตามเส้นด้วย PID Timer ไปข้างหลัง

// >> FFtimer(ความเร็ว,เวลา, 'คำสั่งเจอแยก');  // วิ่งตามเส้นด้วย PID Timer ไปข้างหน้า
// >> BBtimer(ความเร็ว,เวลา, 'คำสั่งเจอแยก');  // วิ่งตามเส้นด้วย PID Timer ไปข้างหลัง

// >> FFWhite(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าจนกว่าเซนเซอร์หน้าจะเจอขาวทั้งหมด
// >> BBWhite(ความเร็ว, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหลังจนกว่าเซนเซอร์หลังจะเจอขาวทั้งหมด
// >> FFBlack(ความเร็วมอเตอร์ซ้าย,ความเร็วมอเตอร์ขวา, 'คำสั่งเจอแยก');  // เคลื่อนที่ไปด้านหน้าจนกว่าเซนเซอร์หน้าจะเจอเส้นสีดำ
// >> BBBlack(ความเร็วมอเตอร์ซ้าย,ความเร็วมอเตอร์ขวา, 'คำสั่งเจอแยก');  // เคลื่อนที่ไปด้านหลังจนกว่าเซนเซอร์หลังจะเจอเส้นสีดำ

////////////////////////////////// คำสั่ง Servo //////////////////////////////////////////////////////////////////
// setOpen(องศาด้านซ้าย, องศาด้านขวา);
// setClose(องศาด้านซ้าย, องศาด้านขวา);
// setCloseSmall(องศาด้านซ้าย, องศาด้านขวา);
// setUpDown(องศาขึ้น, องศาลง);

//////////// SET SERVO SPEED ////////////
/// SERVO_SLOW   << SERVO RUN IN SLOW SPEED
/// SERVO_MEDIUM << SERVO RUN IN MEDIUM SPEED
/// SERVO_FAST   << SERVO RUN IN FAST SPEED
//  setServoSpeed_Up(SERVO_FAST);
//  setServoSpeed_Down(SERVO_MEDIUM);
//  setServoSpeed_Open(SERVO_FAST);
//  setServoSpeed_Close(SERVO_MEDIUM);
/////////////////////////////////////////

// ServoOpen();   // อ้าที่คีบ
// ServoOpen2();   // อ้าที่คีบ
// ServoClose();  // หนีบที่คีบ
// ServoUp();     // ยกที่คีบ
// ServoDown();   // วางที่คีบ
// ServoOpenMax();   // อ้าที่คีบ
// ServoOpenMax2();   // อ้าที่คีบ
// ServoLOpen(); // ที่คีบซ้ายเปิด
// ServoROpen(); // ที่คีบขวาเปิด

/* * IMU / Gyroscope
 * ----------------------------------------------------------------------------------------------
 * setAngleOffset();                // รีเซ็ตมุมหุ่นยนต์ให้เป็น 0 (ควรทำตอนหุ่นอยู่นิ่ง)
 * SetRobotAngle();                 // อ่านค่าองศาปัจจุบันไปเก็บไว้ใช้อ้างอิง

// >> FFtimerG(ความเร็ว,เวลา);  // วิ่งด้วย PID Timer Gyro ไปข้างหน้า
// >> BBtimerG(ความเร็ว,เวลา);  // วิ่งด้วย PID Timer Gyro ไปข้างหลัง
// >> FFBG(ความเร็ว, 'คำสั่งเจอแยก');  // เคลื่อนที่ไปด้านหน้าด้วย Gyro จนกว่าเซนเซอร์หน้าจะเจอเส้นสีดำ
// >> BBBG(ความเร็ว, 'คำสั่งเจอแยก');  // เคลื่อนที่ไปด้านหลังด้วย Gyro จนกว่าเซนเซอร์หลังจะเจอเส้นสีดำ


 * * SpinLG(องศา); / SpinRG(องศา);    // หมุนซ้าย/ขวาด้วยล้อสองข้าง แบบกำหนดองศาเป๊ะๆ
 * // ตัวอย่าง: SpinLG(90); -> หมุนซ้าย 90 องศา
 * TurnLG(องศา); / TurnRG(องศา);    // ตีโค้งเลี้ยวหน้าซ้าย/ขวา (ล้อเดียวขยับ) แบบกำหนดองศา
 * TurnLBG(องศา); / TurnRBG(องศา);  // ตีโค้งถอยหลังซ้าย/ขวา แบบกำหนดองศา
 */
/*
 * * SpinLG(ความเร็ว, องศา); / SpinRG(ความเร็ว, องศา);    // หมุนซ้าย/ขวาด้วยล้อสองข้าง แบบกำหนดองศาเป๊ะๆ
 * // ตัวอย่าง: SpinLG(100,90); -> หมุนซ้าย 90 องศา
 * TurnLG(ความเร็ว, องศา); / TurnRG(ความเร็ว, องศา);    // ตีโค้งเลี้ยวหน้าซ้าย/ขวา (ล้อเดียวขยับ) แบบกำหนดองศา
 * TurnLBG(ความเร็ว, องศา); / TurnRBG(ความเร็ว, องศา);  // ตีโค้งถอยหลังซ้าย/ขวา แบบกำหนดองศา
*/
// TurnLG(100, 90); 
// TurnRG(100, 90); 
// TurnRBG(100, 90); 
// TurnLBG(100, 90); 



/* * คำสั่งหมุน/เลี้ยว Gyro แบบกำหนดองศาอิสระ (Degree-based Turn)
 * ----------------------------------------------------------------------------------------------
 * คำสั่งเหล่านี้รับค่าองศาเป็นตัวเลข ทั้งบวกและลบ (-90 ถึง 90)
 *
 * turnDegree(องศา);   // เลี้ยวหน้าด้วยล้อเดียว ค่า + = เลี้ยวขวา, ค่า - = เลี้ยวซ้าย
 * // ตัวอย่าง: turnDegree(90);  -> เลี้ยวขวา 90 องศา
 * // ตัวอย่าง: turnDegree(-90); -> เลี้ยวซ้าย 90 องศา
 *
 * turnDegreeB(องศา);  // เลี้ยวถอยหลังด้วยล้อเดียว ค่า + = เลี้ยวขวา, ค่า - = เลี้ยวซ้าย
 * // ตัวอย่าง: turnDegreeB(-90); -> ถอยหลังเลี้ยวซ้าย 90 องศา
 *
 * SpinDegree(องศา);   // หมุนตัวอยู่กับที่ (ล้อสองข้างสวน) ค่า + = ขวา, ค่า - = ซ้าย
 * // ตัวอย่าง: SpinDegree(-90); -> หมุนซ้าย 90 องศา
 */

/*
 // >> FFC(ความเร็ว, kp,kd,'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่กลาง
// >> FFL(ความเร็ว, kp,kd,'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่ซ้าย
// >> FFL0(ความเร็ว,kp,kd, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์ริมซ้ายสุดตัวที่ 0
// >> FFR(ความเร็ว,kp,kd, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่ขวา
// >> FFR15(ความเร็ว,kp,kd, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์ริมขวาสุดตัวที่ 15
// >> FFNUM(ความเร็ว, 'คำสั่งเจอแยก', 0->7);  // วิ่งตามเส้น PID ไปด้านหน้าเจอแยกให้ทำตามคำสั่ง >> ตำแหน่งเซนเซอร์ที่ใช้จับแยก

// >> BBC(ความเร็ว, kp,kd,'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหลังเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่กลาง
// >> BBL(ความเร็ว, kp,kd,'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหลังเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่ซ้าย
// >> BBL0(ความเร็ว,kp,kd, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหลังเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์ริมซ้ายสุดตัวที่ 0
// >> BBR(ความเร็ว,kp,kd, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหลังเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์คู่ขวา
// >> BBR15(ความเร็ว,kp,kd, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหลังเจอแยกให้ทำตามคำสั่ง >>  ด้านเซนเซอร์ที่ใช้ เซนเซอร์ริมขวาสุดตัวที่ 15
// >> BBNUM(ความเร็ว,kp,kd, 'คำสั่งเจอแยก', 0->7);  // วิ่งตามเส้น PID ไปด้านหลังเจอแยกให้ทำตามคำสั่ง >> ตำแหน่งเซนเซอร์ที่ใช้จับแยก

// >> FFtimer(ความเร็ว,kp,kd,เวลา);  // วิ่งตามเส้นด้วย PID Timer ไปข้างหน้า
// >> BBtimer(ความเร็ว,kp,kd,เวลา);  // วิ่งตามเส้นด้วย PID Timer ไปข้างหลัง

// >> FFtimer(ความเร็ว,kp,kd,เวลา, 'คำสั่งเจอแยก');  // วิ่งตามเส้นด้วย PID Timer ไปข้างหน้า
// >> BBtimer(ความเร็ว,kp,kd,เวลา, 'คำสั่งเจอแยก');  // วิ่งตามเส้นด้วย PID Timer ไปข้างหลัง

// >> FFWhite(ความเร็ว,kp,kd, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหน้าจนกว่าเซนเซอร์หน้าจะเจอขาวทั้งหมด
// >> BBWhite(ความเร็ว,kp,kd, 'คำสั่งเจอแยก');  // วิ่งตามเส้น PID ไปด้านหลังจนกว่าเซนเซอร์หลังจะเจอขาวทั้งหมด
*/